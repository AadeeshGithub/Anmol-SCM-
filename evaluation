
#include <stdio.h>

int main() {
    int N, K;
    
    // Input the size of the array
    printf("Enter the size of the array: ");
    scanf("%d", &N);
    
    // Input the elements of the array
    int arr[N];
    printf("Enter %d for N no. integers:\n", N);
    for (int i = 0; i < N; i++) {
        scanf("%d", &arr[i]);
    }
    
 hello, i am making scm file
hiiii
how are you
difjdjdjdfjdjdfjjdfjoj
